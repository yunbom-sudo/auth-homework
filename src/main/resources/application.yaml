spring:
  jwt:
    secret: ${JWT_SECRET}
  application:
    name: homework
  datasource:
    url: jdbc:mysql://localhost:3309/homework
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create-drop